// alert ("HELLO").

// --1. ADDDITION
// -- INFO - je peut déclarer plusieurs variables à la suite

// je déclare mes variables
var nb1, nb2, resultat;

// j'affecte des valeurs

nb1 = 10;
nb2 = 5;

// j'utilise mes variables avec un calcul
resultat = nb1 + nb2;

// j'affiche mon résultat dans la console
console.log(resultat);

// --2. Soustaction
// soustaction de nb1 - nb2 avec l'opérateur "-"

resultat = nb1 -nb2;
console.log(resultat);

// -- 3. Multiplication
// Multiplication de nb1 * nb2 avec l'opérateur "*"

resultat = nb1 / nb2;
console.log(resultat);

// -- 5.Modulo
// Le modula retourne le reste d'une dicision
// modulo de nb1 % nb2 avec l'opérateur "%"

resultat = nb1 % nb2;
console.log(resultat);

// et maintenant je ré-affecte une valeur à nb1

nb = 11;
resultat = nb1 % nb2;
console.log("Le reste de la division de " + nb1 + "par" + nb2 +" est égal à : " + resultat);

// --6. Ecritures simplifiées
nb1 = 15;
nb1 = nb1 + 5;// egal à 20

nb1 += 10 ;/// nb1 lui-même +10
console.log('le resultat de nb1 += 10; " est' + nb1);
// égal à 300, alternance de guillemet simples et doubles pour ne pas cassser ma chaine de caractères le dernier montant nb1 + 10 

// si j'utilise une ' (apostrophe) dans une phrase concaténée, je peux échapper le caractère à l'aide d'un anti-slach \ (alt gr + 8 )







